name: CI

on:
  push:
    branches: ["**"]
  pull_request:

# One in-flight run per branch; a new push cancels the previous run.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Formatting and lint are platform- and Node-independent, so this runs once
  # rather than on all four legs of the matrix below. It also fails in well
  # under a minute, which the matrix cannot: @pydantic/monty and the fd/rg
  # install dominate that job's runtime.
  lint:
    name: lint + format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # `biome check` covers the formatter, the linter and the import-sorting
      # assist in one pass. --error-on-warnings is what makes it a gate: Biome
      # exits 0 on warning-severity diagnostics by default, so without it a
      # rule like noExplicitAny would print and pass — a warning tier nobody
      # reads is the same "green means nothing" failure bucket 1 exists to
      # eliminate.
      - run: npm run lint

  # Per-file line-coverage floors (#25). One job, one Node version, one OS:
  # the floors are exact measured numbers, and V8 line attribution differs
  # enough between Node majors that a shared baseline across the matrix would
  # have to be slackened until it stopped biting. Pinned to Node 24, the
  # version the baseline was measured on.
  #
  # This is not a quality gate — see the header of scripts/coverage.mjs. #24's
  # mutation score is.
  coverage:
    name: coverage floors (node 24, ubuntu)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      # src/bridge.ts sits at a 100% floor, which is only reachable with fd and
      # rg present — without them those tests skip and the floor breaks. Same
      # install as the matrix job below, same REQUIRE_BRIDGE_TOOLS=1 reasoning.
      - name: Install fd and ripgrep
        run: sudo apt-get update -qq && sudo apt-get install -y fd-find ripgrep

      - run: npm ci

      - run: npm run coverage
        env:
          REQUIRE_BRIDGE_TOOLS: "1"

  check:
    name: check + test (node ${{ matrix.node }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      # Report every leg: @pydantic/monty ships per-platform native binaries,
      # so one failing leg says nothing about the others.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [22, 24]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      # The bridged find/grep tools shell out to fd and rg. The suite no longer
      # downloads them — test/support/bridge-tools.ts sets PI_OFFLINE=1, so
      # pi's ensureTool() never reaches api.github.com mid-run (#91). Install
      # them here so CI exercises the real integration rather than skipping it.
      - name: Install fd and ripgrep (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update -qq && sudo apt-get install -y fd-find ripgrep

      - name: Install fd and ripgrep (macOS)
        if: runner.os == 'macOS'
        run: brew install fd ripgrep

      - run: npm ci

      - run: npm run check

      # REQUIRE_BRIDGE_TOOLS=1 turns the local "skip if absent" behaviour into a
      # hard failure. Without it, a broken install step above would silently
      # drop the find/grep tests and CI would still be green — the exact
      # "green means nothing" failure bucket 1 exists to eliminate.
      - run: npm test
        env:
          REQUIRE_BRIDGE_TOOLS: "1"
