# Tasks — post-merge micro-flight: close #145's two open items (D28, D29)

- [x] T1 — `Session.prefixLineCount()` O(n²) → incremental running total (D28)
- [x] T2 — rename to `correctDiagnosticText` + references (D29)
- [x] T3 — pin the three surviving prefixLineCount counter sites (VERIFY finding, test-only)
